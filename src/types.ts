export interface HotlineEntry {
  id: string;
  image?: string;
  serviceType: string;
  name: string;
  designation: string;
  address: string;
  mobile: string;
  email: string;
  createdAt: number;
}
